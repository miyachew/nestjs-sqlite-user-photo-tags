version: '3'
services:
  app:
    # restart: always
    build: ./
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    # links:
    #   - db_server
  # db_server:
  #   image: nouchka/sqlite3:latest
  #   stdin_open: true
  #   tty: true
  #   volumes:
  #     - ./db/:/root/db/